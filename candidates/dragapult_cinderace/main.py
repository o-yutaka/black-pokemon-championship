from __future__ import annotations

raise RuntimeError(
    "NON_CANONICAL_ENTRYPOINT: dragapult_cinderace must be opened only through "
    "the exact submission-root main.py + deck.csv + black_engine + black_lab.py + cg layout"
)
